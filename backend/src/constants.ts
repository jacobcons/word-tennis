export const COUNTDOWN_TIME_S = 3;
export const TURN_TIME_S = 5;
export const IS_VALID_WORD_PROMPT = `You are given a word. You must output y if its spelt correctly, the corrected word if a spell checker would correct it (e.g. rasberry->raspberry), or n if it's spelt incorrectly. The output must not exceed a single word.`;
export const GAME_DATA_TTL_S = 3600;
